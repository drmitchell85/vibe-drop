# Environment Configuration
# Options: dev, staging, prod
ENVIRONMENT=dev

# API Gateway Configuration
API_GATEWAY_PORT=8080
# Required: URL where the File Service is running
FILE_SERVICE_URL=http://localhost:8081

# File Service Configuration  
FILE_SERVICE_PORT=8081
# Required: S3 bucket name (must exist or be created)
S3_BUCKET=vibe-drop-bucket
# AWS region (defaults based on environment)
S3_REGION=us-east-1
# S3 endpoint (only set for LocalStack in dev, leave empty for real AWS)
S3_ENDPOINT=http://localhost:4566

# Development Setup:
# 1. Start LocalStack: docker run --rm -p 4566:4566 localstack/localstack
# 2. Create bucket: aws --endpoint-url=http://localhost:4566 s3 mb s3://vibe-drop-bucket
# 3. Set S3_BUCKET=vibe-drop-bucket and S3_ENDPOINT=http://localhost:4566

# Production/Staging Setup:
# 1. Set ENVIRONMENT=prod or ENVIRONMENT=staging
# 2. Set S3_BUCKET to your actual AWS bucket name
# 3. Remove or leave S3_ENDPOINT empty to use default AWS endpoints
# 4. Ensure AWS credentials are configured (IAM roles, ~/.aws/credentials, etc.)